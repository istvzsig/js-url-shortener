export class default ShortyUrl {
      constructor(url, shorty) {
            this._url = url;
            this._shorty = shorty;
      }

      static save() {

      }

      static load() {
            
      }
}